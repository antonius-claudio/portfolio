import React from "react";
import Style from "./Portfolio.module.scss";

export const Portfolio = (props) => {
  return <div className={Style.ortfolio}>
    <div className={Style.wrapperPortfolio}>
      Portfolio
    </div>
  </div>;
};